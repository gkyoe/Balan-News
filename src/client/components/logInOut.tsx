import * as React from "react";

// interface LogProps {
//   btn: string;
// }

const LogInOut: React.FC = () => {
  return (
    <div>
      <button>로그인</button>
      <button>회원가입</button>
    </div>
  );
};

export default LogInOut;
